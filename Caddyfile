:80 {
    # Proxy all /api/* requests to the Quarkus backend
    handle /api/* {
        reverse_proxy backend:8080
    }

    # Serve frontend static files (when frontend container is added)
    handle {
        reverse_proxy frontend:3000
    }
}

