meta {
  name: GET /events
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/events
  auth: none
}

body:none {
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
