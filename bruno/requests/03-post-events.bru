meta {
  name: POST /events
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/events
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "eventType": "WORKSHOP",
    "eventDate": "2026-05-20",
    "eventLocation": "Glasgow Hub"
  }
}

tests {
  test("status is 201", function() {
    expect(res.getStatus()).to.equal(201);
  });
}
