meta {
  name: GET /staff
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/staff
  auth: none
}

body:none {
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
